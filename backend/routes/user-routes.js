import express from "express";

// import { createUser, deleteUserById, getAllUsers, getUserById, updateUserById } from "../controller/userController.js";
// import isAuthorizedUSer from "../middleware/authorized-middleware";
// import isAuthenticatedUser from "../middleware/authentication-middleware";

const router = express.Router();

// router.route("/user/account/login").post(login);
// router.route("/user").get(isAuthenticatedUser,isAuthorizedUSer,getAllUsers);
// router.route("/user/new").post(createUser);
// router.route("/user/:id").get(getUserById);
// router.route("/user/update/:id").put(updateUserById);
// router.route("/user/delete/:id").delete(deleteUserById);

export default router;
